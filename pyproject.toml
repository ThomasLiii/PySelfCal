[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "SelfCal"
version = "0.1.0"
description = "Self-calibration pipeline for astronomical imaging"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["SelfCal*"]
